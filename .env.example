# Copie este arquivo para .env e ajuste os valores

# Ambiente (desenvolvimento)
NODE_ENV=development
PORT=8000
APP_URL=http://localhost:8000
CHECKOUT_SUCCESS_URL=http://localhost:8000/
CHECKOUT_CANCEL_URL=http://localhost:8000/pages/carrinho.html

# Stripe — use TEST em dev; LIVE somente em produção
# Dev/Homolog: sk_test_...
STRIPE_SECRET_KEY=sk_test_xxx
# Produção: defina a chave no provedor (não commitar valores reais)
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Controle explícito do Pix no Checkout:
# - true  => oferece Pix (com fallback para cartão se inválido)
# - false => não oferece Pix
# Recomendado: false em dev para evitar surpresas
ENABLE_PIX=false

# Controle explícito do Boleto no Checkout:
# - true  => oferece Boleto (com fallback para cartão se inválido)
# - false => não oferece Boleto
# Recomendado: false em dev para evitar surpresas
ENABLE_BOLETO=false

# Prazo de vencimento do Boleto (em dias)
# Valor padrão: 3
BOLETO_EXPIRES_AFTER_DAYS=3

# JWT
JWT_SECRET=change_me
JWT_EXPIRES_IN=7d

# SMTP (opcional). Em dev você pode usar FAKE_MAIL=true
FAKE_MAIL=true
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=no-reply@example.com

#
# Exemplo de produção (Render) — ajuste no painel de env do Render
# Não commitar valores reais no repositório.
#
# APP_URL=https://meuvinhos.onrender.com
# CHECKOUT_SUCCESS_URL=https://meuvinhos.onrender.com/pages/index.html
# CHECKOUT_CANCEL_URL=https://meuvinhos.onrender.com/pages/carrinho.html
# NODE_ENV=production
# STRIPE_SECRET_KEY=<STRIPE_LIVE_SECRET_FROM_DASHBOARD>
# ENABLE_PIX=false
# ENABLE_BOLETO=false
# BOLETO_EXPIRES_AFTER_DAYS=3

# Observação: evite incluir exemplos com o prefixo de chaves reais
# (como "sk_live_") em arquivos versionados, pois isso ativa o
# push protection do GitHub mesmo em comentários.